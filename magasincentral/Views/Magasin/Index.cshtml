@model IEnumerable<magasincentral.Models.Magasin>

<h2>Liste des magasins</h2>

<table class="table">
    <thead>
        <tr>
            <th>Nom</th>
            <th>Quartier</th>
            <th></th> <!-- colonne vide pour actions -->
        </tr>
    </thead>
    <tbody>
@foreach (var magasin in Model)
{
        <tr>
            <td>@magasin.Nom</td>
            <td>@magasin.Quartier</td>
            <td>
                <a asp-action="Details" asp-route-id="@magasin.Id" class="btn btn-info">Détails</a>
            </td>
        </tr>
}
    </tbody>
</table>

<a class="btn btn-primary" asp-action="Create">Ajouter un magasin</a>
